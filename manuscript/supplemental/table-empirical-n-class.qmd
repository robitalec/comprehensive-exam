# Table S3

Table S3: Taxonomic classes in empirical tests of producer-scrounger dynamics

```{r}
#| include: false
library(gt)
```

```{r, echo = FALSE}
targets::tar_read(n_by_class) |>
	gt() |>
	summary_rows(columns = 'Count', fns = c('sum', 'mean', 'median'))
```
